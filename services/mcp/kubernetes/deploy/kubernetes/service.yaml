apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: infragpt
  labels:
    app: mcp-server
spec:
  selector:
    app: mcp-server
  ports:
  - name: api
    port: 8081
    targetPort: api
  - name: metrics
    port: 8080
    targetPort: metrics
  type: ClusterIP